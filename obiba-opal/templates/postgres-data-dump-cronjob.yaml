{{- if .Values.postgres.data.backup.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ printf "%s-dump-cronjob" .Values.postgres.data.name }}
spec:
  schedule: {{ .Values.postgres.data.backup.schedule }}
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: postgres-dump
              image: {{ .Values.postgres.data.image }}
              env:
                - name: POSTGRES_USER
              {{- if or .Values.postgres.data.existingSecret .Values.global.existingSecret }}
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.postgres.data.existingSecret | default .Values.global.existingSecret }}
                      key: {{ .Values.postgres.data.existingSecretKeys.user }}
              {{- else }}
                  value: {{ .Values.postgres.data.user | quote }}
              {{- end }}
                - name: POSTGRES_PASSWORD
              {{- if or .Values.postgres.data.existingSecret .Values.global.existingSecret }}
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.postgres.data.existingSecret | default .Values.global.existingSecret }}
                      key: {{ .Values.postgres.data.existingSecretKeys.password }}
              {{- else }}
                  value: {{ .Values.postgres.data.password | quote }}
              {{- end }}
                - name: POSTGRES_HOST
                  value: {{ .Values.postgres.data.host | quote }}
                - name: POSTGRES_PORT
                  value: {{ .Values.postgres.data.port | quote }}
              command: ["/bin/sh", "-c"]
              args:
                - |
                  BACKUP_DATE=$(date +%F)
                  echo "Starting PostgreSQL data backup for date: $BACKUP_DATE"
                  
                  echo "*:*:*:${POSTGRES_USER}:${POSTGRES_PASSWORD}" > ~/.pgpass
                  chmod 600 ~/.pgpass
                  pg_dumpall -h ${POSTGRES_HOST} -p ${POSTGRES_PORT} -U ${POSTGRES_USER} \
                    | gzip -4 > /dump/postgres-data-$BACKUP_DATE.sql.gz
                  
                  echo "PostgreSQL data backup completed at $(date)"
                  
                  # Clean up old backups based on limit
                  BACKUP_LIMIT={{ .Values.postgres.data.backup.limit | default 10 }}
                  echo "Keeping only $BACKUP_LIMIT most recent PostgreSQL data backups"
                  
                  # Clean up old backup files
                  cd /dump && ls -t postgres-data-*.sql.gz 2>/dev/null | tail -n +$((BACKUP_LIMIT + 1)) | xargs -r rm -f
                  
                  echo "PostgreSQL data backup cleanup completed. Current backups:"
                  ls -la /dump/postgres-data-*.sql.gz 2>/dev/null | sort || echo "No backup files found"
              volumeMounts:
                - name: postgres-dump
                  mountPath: /dump
          volumes:
            - name: postgres-dump
              persistentVolumeClaim:
                claimName: {{ printf "%s-dump-pvc" .Values.postgres.data.name }}
          restartPolicy: OnFailure
{{- end }}