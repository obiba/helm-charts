{{- if .Values.postgres.config.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.postgres.config.name }}
  labels:
    {{- include "postgres.config.labels" . | nindent 4 }}
  {{- with .Values.postgres.config.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.postgres.config.service.type | default "ClusterIP" }}
  ports:
    - port: 5432
  selector:
    {{- include "postgres.config.selectorLabels" . | nindent 4 }}
{{- end }}
