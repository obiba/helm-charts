{{- if .Values.postgres.data.backup.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ printf "%s-dump-pvc" .Values.postgres.data.name }}
  labels:
    {{- include "postgres.data.dump.pvc.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.global.storageClassName }}
  storageClassName: {{ .Values.global.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.postgres.data.backup.pvcSize | default "2Gi" }}
{{- end }}
